name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "29 23 16 * *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@f62ef26d5b2705fbcbf9c48ed4619f055efa3481 # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
