name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "29 23 16 * *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@bf6f6e022e388fdbeb8ab09747fdf15eb4d68a72 # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
