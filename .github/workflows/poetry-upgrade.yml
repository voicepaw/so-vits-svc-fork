name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "29 23 16 * *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@2e443406e88fd4e9f1d23620f0908f425220b38a # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
