name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "29 23 16 * *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@2b9be5e203e76d66c6d7d3b441e03629d1a65a95 # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
