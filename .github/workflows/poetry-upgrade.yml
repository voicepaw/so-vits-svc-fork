name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "29 23 16 * *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@3ceed0e5ad20c6d9e64f46937b7c0425bbe51b54 # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
