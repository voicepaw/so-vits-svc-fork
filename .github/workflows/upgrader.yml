name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "15 11 3 1-9,11-12 *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/uv-upgrade.yml@2e443406e88fd4e9f1d23620f0908f425220b38a # v1
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
