name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "15 11 3 1-9,11-12 *"

jobs:
  upgrade:
    uses: browniebroke/github-actions/.github/workflows/uv-upgrade.yml@8fb2a2c1a48f4a3d627edcfe4355c734cee9f0c1 # v2
    secrets:
      gh_pat: ${{ secrets.GH_PAT }}
